const express = require("express");
const router = express.Router();
const emailControllers = require("../../controllers/util/emailer");

// router.get("/send", emailControllers.sendEmail);

module.exports = router;